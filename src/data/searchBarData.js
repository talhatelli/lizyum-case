import { colors } from '../utils/colors';

export const searchBarDefaults = {
  defaultPlaceholder: "Aramak İstediğiniz Hizmeti Yazın",
  defaultBackgroundColor: colors.white,
  defaultIconBackgroundColor: colors.lightBlue,
  defaultWithFilter: true
};

export default searchBarDefaults; 